<form {{action 'updateExample' on="submit"}}>
  <label for="example">Example</label>
  {{input placeholder="Example" value=example.text}}
  <button type="submit">Save</button>
  {{#if editForm}}
    <button {{action 'cancel'}}>Cancel</button>
  {{/if}}
</form>
